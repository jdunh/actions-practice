name: heroku
on:
  push:
jobs:
  deploy:
    runs-on: ubuntu-latest
    container:
      image: cimg/base:stable
    steps:
    - uses: actions/checkout@v2
    - run: 'echo "The command above installs Heroku, the command below deploys. What you do inbetween is up to you!"'
